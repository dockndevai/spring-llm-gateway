<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.3</version>
    <relativePath/>
  </parent>

  <groupId>io.github.dockndevai</groupId>
  <artifactId>spring-llm-gateway-parent</artifactId>
  <version>0.1.0</version>
  <packaging>pom</packaging>

  <name>spring-llm-gateway-parent</name>
  <description>LLM gateway capabilities on top of Spring Cloud Gateway Server WebFlux</description>
  <url>https://github.com/dockndevai/spring-llm-gateway</url>
  <inceptionYear>2026</inceptionYear>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>dockndevai</id>
      <name>Ankit Verma</name>
      <url>https://github.com/dockndevai</url>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:https://github.com/dockndevai/spring-llm-gateway.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/dockndevai/spring-llm-gateway.git</developerConnection>
    <url>https://github.com/dockndevai/spring-llm-gateway</url>
    <tag>HEAD</tag>
  </scm>

  <modules>
    <module>spring-llm-gateway-core</module>
    <module>spring-llm-gateway-sample</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-cloud.version>2025.0.0</spring-cloud.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <profiles>
    <!--
      Everything Maven Central requires, kept out of the default build so day-to-day
      "mvn verify" stays fast and needs no GPG key. Activate with -Prelease.
    -->
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>3.4.0</version>
            <executions>
              <execution>
                <id>attach-sources</id>
                <goals><goal>jar-no-fork</goal></goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>3.12.0</version>
            <executions>
              <execution>
                <id>attach-javadocs</id>
                <goals><goal>jar</goal></goals>
              </execution>
            </executions>
            <configuration>
              <!-- Central requires a javadoc jar, not perfect javadoc. Do not fail the
                   release on a missing @param in an internal class. -->
              <doclint>none</doclint>
              <quiet>true</quiet>
            </configuration>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.2.8</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals><goal>sign</goal></goals>
              </execution>
            </executions>
            <configuration>
              <!-- Reads the passphrase from settings.xml rather than prompting, so the
                   same command works locally and in CI. -->
              <gpgArguments>
                <arg>--pinentry-mode</arg>
                <arg>loopback</arg>
              </gpgArguments>
            </configuration>
          </plugin>
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>0.11.0</version>
            <extensions>true</extensions>
            <configuration>
              <!-- Must match a <server><id> in ~/.m2/settings.xml holding the
                   Central portal user token. -->
              <publishingServerId>central</publishingServerId>
              <!-- Deliberately false: the upload is staged and validated, and you press
                   publish in the portal. A published version can never be deleted. -->
              <autoPublish>false</autoPublish>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <!--
              JDK 23+ no longer runs annotation processors discovered on the classpath unless
              asked. Without this, spring-boot-configuration-processor is silently skipped and
              no spring-configuration-metadata.json is produced, so IDE completion for
              llm.gateway.* would not work.
            -->
            <proc>full</proc>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <configuration>
            <argLine>-XX:+EnableDynamicAgentLoading</argLine>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
